import { ex02 } from '../ex02'

describe('02. "shoe" + "cold" = "schooled"', () => {
  it('the letters in "shoe" and "cold" one after the other from head to tail', () => {
    expect(ex02()).toBe('schooled')
  })
})
